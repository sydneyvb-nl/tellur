# Tellur JetBrains plugin build properties
pluginGroup=dev.tellur.jetbrains
pluginVersion=0.1.0

# Compile against the oldest supported platform while declaring the reviewed
# upper compatibility branch. Cross-version Plugin Verifier coverage is tracked
# alongside the required Gradle/IntelliJ build-tool upgrade.
platformType=IC
platformVersion=2024.1
pluginSinceBuild=241
pluginUntilBuild=253.*

org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
kotlin.stdlib.default.dependency=false
