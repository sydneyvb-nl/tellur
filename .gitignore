node_modules/
.turbo/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
coverage/
*.log

# Build output of the VS Code extension (TypeScript)
editor/tellur-vscode/out/
editor/tellur-vscode/*.vsix

# Rust
target/
**/*.rs.bk
# Cargo.lock IS committed for the CLI binary (reproducible, auditable builds).

# Tellur local data (logs + index are machine-local; config/policies are committed)
.tellur/traces/
.tellur/index/
.tellur/daemon.token
