{
  "name": "@tracegit/cli",
  "version": "0.0.1",
  "description": "TraceGit CLI — AI code provenance from the terminal",
  "bin": {
    "tracegit": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracegit/core": "*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "license": "Apache-2.0"
}
